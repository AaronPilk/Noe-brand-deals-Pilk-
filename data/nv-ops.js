/* Optional repo-committed operational baseline.
 * To carry your stages/notes/approvals across devices (or back them up in git):
 * Settings → Export operational data, then replace `null` below with the exported JSON
 * and commit. A fresh browser seeds from this; local changes still layer on top. */
window.NV_OPS = null;
